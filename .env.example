# -----------------------------------------------------------------------------
# Core App / Supabase
# -----------------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Storage bucket for generated PPT files
SUPABASE_BUCKET=ppt

# Existing app storage bucket (if used by supplier uploads)
NEXT_PUBLIC_SUPABASE_STORAGE_BUCKET=supplier-files

# -----------------------------------------------------------------------------
# Gamma API PPT Generation (active)
# -----------------------------------------------------------------------------
GAMMA_BASE_URL=https://public-api.gamma.app/v1.0
GAMMA_BASE_URLS=
GAMMA_API_KEY=
GAMMA_TEXT_MODE=preserve
GAMMA_EXPORT_AS=pptx
GAMMA_THEME_ID=
GAMMA_FOLDER_IDS=
GAMMA_FORCE_IPV4=true
GAMMA_PROXY_URL=
GAMMA_USE_ENV_PROXY=false
GAMMA_STATUS_USE_CURL=true
GAMMA_CONNECT_TIMEOUT_MS=10000
GAMMA_CREATE_TIMEOUT_MS=25000
GAMMA_STATUS_TIMEOUT_MS=30000
GAMMA_DOWNLOAD_TIMEOUT_MS=35000
GAMMA_PENDING_RETRY_AFTER_SEC=180
GAMMA_PENDING_MAX_RETRY=1
GAMMA_MAX_POLL_FAILURES=3
GAMMA_MAX_TRANSIENT_POLL_FAILURES=10
GAMMA_COVER_AI_BACKGROUND=true

# -----------------------------------------------------------------------------
# Legacy LLM/PPT variables (kept for rollback only)
# -----------------------------------------------------------------------------
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Optional render/guardrail controls
PPT_THEME_NAME=corporate
STRICT_ACCURACY=true
SHOW_SOURCE_NOTES=true
ENABLE_IMAGE_PROVIDER=false
PPT_AGENT_BUDGET_MS=24000
PPT_AGENT_STAGE_TIMEOUT_MS=35000

# Optional but recommended for securing /api/ppt/worker
CRON_SECRET=

# -----------------------------------------------------------------------------
# Optional services used elsewhere in this project
# -----------------------------------------------------------------------------
RESEND_API_KEY=
RESEND_FROM_EMAIL=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
